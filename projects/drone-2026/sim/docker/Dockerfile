# syntax=docker/dockerfile:1.7
# Compatibility alias for older commands that referenced a single simulation Dockerfile directly.
# Docker Compose now builds robot.Dockerfile, which extends simulator.Dockerfile.
FROM drone-2026-robot:local
