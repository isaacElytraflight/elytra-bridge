# Drone 2026 simulator package

The MVP points at the existing `drone-2026/SITL/web-sim/docker-compose.yml` stack. Set `SIM_COMPOSE_FILE` in `application/backend/.env` if the reference repository is checked out elsewhere.
